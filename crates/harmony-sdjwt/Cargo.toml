[package]
name = "harmony-sdjwt"
description = "SD-JWT (RFC 9901) parser and JWS signature verification"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
harmony-identity = { workspace = true }
harmony-credential = { workspace = true, optional = true }
harmony-crypto = { workspace = true, optional = true }
serde_json = { workspace = true, optional = true }
sha2 = { workspace = true, optional = true }
base64 = { workspace = true, features = ["alloc"] }

[features]
default = ["std"]
std = ["dep:serde_json", "serde_json?/std", "harmony-identity/std", "base64/std"]
credential = ["dep:sha2", "dep:harmony-credential", "dep:harmony-crypto", "std"]

[dev-dependencies]
serde_json = { workspace = true }
rand = { workspace = true }
