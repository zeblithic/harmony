// Stub. Filled in by ZEB-175 Task 14 (wire-format fixture round-trip test).
// Only compiled when the `test-fixtures` feature is enabled — see the
// `[[test]] required-features = ["test-fixtures"]` declaration in Cargo.toml.
